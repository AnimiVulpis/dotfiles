# `configs` folder

This contains backups of config values that could get lost otherwise

## LeaderKey

Should work with minor (path) modifications

## VSCode

Should work with minor (path) modifications
